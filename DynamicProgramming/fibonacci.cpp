#include <iostream>
#include <vector>
using namespace std;

int solve(int n, vector<int> dp)
{
    if (in)
}

int main()
{
    int n;
    cin >> n;

    vector<int> dp(n + 1);

    for (int i = 0; i <= n; i++)
        dp[i] = -1;

    cout << solve(n, dp) << endl;
    return 0;
}